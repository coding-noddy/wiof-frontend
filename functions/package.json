{
  "name": "wiof-functions",
  "version": "1.0.0",
  "description": "WIOF Firebase Cloud Functions - Dynamic OG meta tags for social sharing",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.9.0"
  }
}
